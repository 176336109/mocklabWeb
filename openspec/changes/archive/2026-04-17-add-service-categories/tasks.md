## 1. Modify Configuration

- [x] 1.1 Update `content/services/technical-co-running.md` front matter to include `menu="main"` and `weight=10` with title "创业技术陪跑".
- [x] 1.2 Update `content/services/agent-transformation.md` front matter to include `menu="main"` and `weight=20` with title "企业Agent构建".
